# FastAPI and web server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Database and authentication
supabase>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Data processing and analysis - using compatible versions for Python 3.12
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Authentication and security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0
python-dotenv>=1.0.0

# File handling
Pillow>=10.0.0

# HTTP client
httpx>=0.25.0

# Development and testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# Core RSB Combinator dependencies (from existing system)
# These are already in your existing system
# customtkinter  # Desktop UI (not needed for API)
# tkinterdnd2   # Desktop drag-drop (not needed for API)
